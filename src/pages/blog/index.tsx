import React from "react";
import { PageProps } from "gatsby";

import { Layout } from "../../components/Layout";

function BlogPage(props: PageProps) {
  return (
    <Layout>
      Blog Page
    </Layout>
  )
}

export default BlogPage;